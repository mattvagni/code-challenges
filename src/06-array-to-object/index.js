/**
 * This should take a list of arrays (where each array has two things in it)
 * and return an object with each of those arrays as a key and value.
 *
 * So for example an array like this: [['a', 1], ['b', 2]] should become:
 *
 *    {
 *       a: 1,
 *       b: 2
 *    }
 */
function arrayToObject(listOfArrays) {

}

module.exports = arrayToObject;
