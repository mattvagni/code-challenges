/**
 * This function adds 2 to the number given.
 *
 * This is the only function i've implemented to show you how the
 * test and the setup works.
 */
function addTwo(x) {
    return x + 2;
}

module.exports = addTwo;
