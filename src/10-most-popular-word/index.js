/**
 * This function takes a bit of text.
 *
 * It should return the most common word in that bit of text.
 *
 * Small gotcha: it should _not_ be case sensitive.
 */
function mostPopularWord(speech) {

}

module.exports = mostPopularWord;
