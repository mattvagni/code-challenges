/**
 * This one is kind of tricky to explain.
 *
 * This is a function that makes a new function :tada:
 *
 * The first time you call it with a number it should give you a
 * new function that will always add that first number when called.
 *
 * If you read the tests you'll know what I mean I hope. Literally don't
 * know how to explain this better.
 */
function createAddFunction(numberToAdd) {
    
}

module.exports = createAddFunction;
